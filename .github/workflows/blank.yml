name: CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: mwflaher/gh-rudderstack-action
        with:
          name: BuildStarted
          user: Matt
