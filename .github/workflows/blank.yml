name: CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: mwflaher/gh-rudderstack-action@0.1.0
        with:
          name: BuildStarted
          user: Matt
